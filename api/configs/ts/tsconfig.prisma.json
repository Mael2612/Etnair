{

    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "sourceMap": false,    /* Create source map files for emitted JavaScript files. */
        "rootDir": "../../prisma", /* Specify the root folder within your source files. */
        "outDir": "../../dist/prisma" /* Specify an output folder for all emitted files. */,
    },
    "exclude": ["../../src/**/*.ts"]
}